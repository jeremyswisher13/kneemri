# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw MRI screenshot uploads (staging only — processed/de-identified copies go in public/images)
mri-staging/

# Firebase deploy cache (build artifact)
.firebase/

# Environment files (secrets / Firebase web config). *.local already covers
# .env.local; this also catches a bare .env in the repo root or functions/.
.env
